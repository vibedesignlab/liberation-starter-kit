# Stage 3 R5 — Fig Leaf Square Product

## Input authority

- **Image 1:** `r18-07-thyme-honey-plain-ui-cultured-cream-square.png`, the exact edit target and authority for strict-front jar geometry, open-mouth silhouette, transparent glass, scale, x/y coordinates, full 1:1 composition, partial Batch Record paper/structure/position, compact contact shadow and uniform runtime UI field `#F5F1E8`.
- Preserve Image 1 as a template, not inspiration. Change only the food's verified Fig Leaf visual identity and the Batch Record's `RECIPE` value.

## Built-in imagegen edit prompt

Use case: precise-object-edit.

Asset type: Stage 3 landing product-grid / PDP-primary square product family frame.

Primary request: change only two linked details in Image 1: (1) replace the Thyme Honey food identity inside the existing jar with the Fig Leaf recipe identity; (2) replace only the recipe-name value `THYME HONEY` with the exact text `FIG LEAF`. Keep everything else exactly unchanged.

Food change: preserve the same dense cultured-milk base, fill level, surface, glass occlusion and edible texture. Make the yogurt near-ivory with one restrained, thin green-beige recipe trace inside the yogurt at the same physical level as the original honey fold. The trace is muted, sparse and food-native, never bright green, never a whole leaf and never garnish. This is a directional/release-blocked visual identity only; add no launch, safety or performance claim.

Exact preserve lock: identical jar outline, low-wide proportions, diameter, height, base, open mouth, rim, screw threads, glass thickness, refraction, front face, centered coordinates, occupancy, complete silhouette and neutral product perspective; identical partial uncoated-paper Batch Record size, attachment, coverage, color, hierarchy, type style, line positions and maker-check mark; identical compact shadow immediately below the jar; identical flat edge-to-edge `#F5F1E8` field with no texture, horizon, gradient or vignette. Preserve jar and shadow inside the central 72% with at least 12% margin on all sides and the template's approximately 42–50% frame-height intent.

Text lock: preserve `MORA`, `BATCH RECORD`, `RECIPE`, `MILK`, `CULTURE`, `STRAINER`, `MAKER CHECK` and the existing check mark exactly once in their existing positions. Replace only `THYME HONEY` with `FIG LEAF`. No other added, deleted, duplicated, corrupted or invented text, dates, codes, numbers or marks.

Avoid: camera/crop/scale shift, jar redesign, label redesign, label movement, full-wrap label, bright green yogurt, whole leaf, leaf garnish, herb sprig, honey seam, external ingredient, cap, spoon, prop, second object, floor, wall, horizon, seam, texture, paper background, gradient, warm cast, excessive shadow, floating jar, CGI glass, added logo or watermark.

## Role QA

- One complete strict-front Fig Leaf jar in the same family coordinates and scale.
- Near-ivory yogurt with restrained green-beige trace; no bright green or leaf garnish.
- Uniform `#F5F1E8`; exact partial Batch Record with only `RECIPE` value changed.

## Delivery protocol

- Preserve the built-in generated PNG untouched under `assets/revision-r5-layout-aware/native/products/`.
- Create the matching `assets/revision-r5-layout-aware/products/` PNG with `sips -z 2048 2048`.
- The 2048 × 2048 delivery file is a high-quality resample from the native raster, not a new render and not new image detail.

## Delivery record

- Native: `assets/revision-r5-layout-aware/native/products/st3-r5-product-fig-leaf-native.png`, 1254 × 1254 PNG, SHA-256 `e8d300805133dafecce02aad13bc48b6b884c92ecac945f13b32976e430b50f4`.
- Delivery: `assets/revision-r5-layout-aware/products/st3-r5-product-fig-leaf.png`, 2048 × 2048 PNG, SHA-256 `c0f1943e25ec6abed989e707281cd8d43ec3b564b28925a8be05645ed762a119`.
- Delivery was produced from the untouched native PNG with `sips -z 2048 2048`; it is a resample, not a new render and not new detail.
